Redis持久化方式（默认为RDB）
	1.RDB：每隔一段时间把内存数据以快照的形式保存到硬盘(每隔5分钟甚至更久)
	2.AOF：将每一个写命令都写入一个AOF日志文件中，恢复数据时执行文件中的命令

你们的Redis怎么持久化的
	master关闭持久化
	slave开RDB即可，必要的时候AOF和RDB都开启（AOF重构数据慢一些）



为何要AOF和RDB都开启？
	每次RDB耗时较长，间隔时间短了，会严重影响性能。间隔时间长了，宕机会导致大量数据丢失。
	加上AOF。redis重启时，会使用RDB持久化文件重新构建内存，再使用AOF重放近期的操作指令