索引，缓存，sql优化，表结构优化，数据库优化等

（1）优化SQL，合理使用索引
（2）尝试使用缓存，不要让数据都走数据库

SQL优化

表结构优化
1、将字段很多的表分解为多个表（使用频率低的字段分拆出去）
2、增加中间表（存放需要经常join的数据）

数据库优化
1.数据库读写分离（减少IO压力，由于数据库对记录做了持久化并存储在磁盘上，对磁盘的I/O又是非常消耗性能的操作，因此读、写都在一个库中会大大增加I/O的压力）

慢查询

1.缓存
读取频繁，很少变更的数据缓存入内存
缓解数据库的读取压力

2.主从读写分离
读写压力都很大的情况下，使用主从复制技术来达到读写分离（分库）