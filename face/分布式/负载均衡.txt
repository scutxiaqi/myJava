
0.负载均衡（用Nginx做前端服务器）
是整个集群系统的前端，负责把客户请求转发到Real Server上；Load Balancer通过Ldirectord监测各Real Server的健康状况。在Real Server不可用时把它从群中剔除，恢复时重新加入。

算法
   1）随机（按权重设置随机概率，好机器权重大一些）（默认）
   2）轮询
   3）最小连接（空闲的机器被调用） 
   4）

